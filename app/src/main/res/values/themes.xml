<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GuidomiaX" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GuidomiaX" parent="Base.Theme.GuidomiaX" />

    <style name="MyRatingBarStyle" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/star_icon</item>
        <item name="android:minHeight">15dip</item>
        <item name="android:maxHeight">47dp</item>
        <item name="android:padding">2dp</item>
    </style>
</resources>